<%= form_for(schedule, remote: true) do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
        <% @schedule.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id, :value => current_user.id %>

  <%= f.text_field :name, :placeholder => 'burndown title', :id => 'new_schedule_name_field', :class => 'new_schedule_field' %>
  <%= f.text_field :hours_allotted, :placeholder => 'hours', :id => 'new_schedule_hours_field', :class => 'new_schedule_field' %>
  <%= f.text_field :notes, :placeholder => 'notes', :id => 'new_schedule_notes_field', :class => 'new_schedule_field' %>

  <%= f.submit 'ok!', :id => 'new_schedule_submit' %>
<% end %>


